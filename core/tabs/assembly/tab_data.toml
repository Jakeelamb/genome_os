name = "Assembly"

[[data]]
name = "Reference genome"
multi_select = false

[[data.entries]]
name = "GRCh38 bundle status"
description = "Checks configured GATK GRCh38 reference, known-sites resources, and BWA index files."
script = "scripts/reference_bundle_status.sh"
task_list = "CHK REF"
multi_select = false

[[data.entries]]
name = "Fetch GATK GRCh38 resource bundle"
description = "Downloads public Broad/GATK GRCh38 FASTA and known-sites files into the local Open Genome cache."
script = "scripts/fetch_gatk_grch38_bundle.sh"
task_list = "REF DATA"
multi_select = false

[[data.entries]]
name = "Index configured GRCh38 reference"
description = "Runs samtools faidx, GATK CreateSequenceDictionary, and bwa index through the opengenome environment."
script = "scripts/index_grch38_reference.sh"
task_list = "REF PIPE"
multi_select = false

[[data]]
name = "Sarek germline workflow"
multi_select = false

[[data.entries]]
name = "Check Nextflow / Java"
description = "Shows Nextflow and Java availability before preparing a run."
script = "scripts/check_nextflow.sh"
task_list = "CHK PIPE"
multi_select = false

[[data.entries]]
name = "Prepare Sarek germline run"
description = "Generates a local nf-core/sarek command using the scanned samplesheet and configured GRCh38 resources."
script = "scripts/prepare_sarek_run.sh"
task_list = "CFG PIPE"
multi_select = false

[[data.entries]]
name = "Run / resume Sarek germline workflow"
description = "Runs the generated local nf-core/sarek command with -resume and the conda profile."
script = "scripts/run_sarek.sh"
task_list = "PIPE DATA"
multi_select = false
